{
	"name": "@trp/db",
	"version": "1.0.0",
	"description": "DB access for TRP framework (PostgreSQL + Drizzle + pg)",
	"main": "dist/index.js",
	"scripts": {
		"build": "echo Build is handled by workspace build-scripts.mjs",
		"db:generate": "drizzle-kit generate",
		"db:migrate": "drizzle-kit migrate"
	},
	"dependencies": {
		"drizzle-orm": "^0.33.0",
		"mysql2": "^3.14.3",
		"zod": "^3.23.8"
	},
	"devDependencies": {
		"drizzle-kit": "^0.24.0"
	}
}
